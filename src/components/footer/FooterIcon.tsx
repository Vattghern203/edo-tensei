import { HTMLAttributes } from "react"

interface FooterIconProps {

    type: "svg" | "img" | "htmlClass" | "fontComponent"
}

const FooterIcon = ( { type }:FooterIconProps ) => {

    return (
        ""
    )
}